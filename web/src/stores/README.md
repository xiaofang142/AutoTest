# Pinia Stores

Create store modules here for state management:

- projectStore.ts — Project CRUD state
- runStore.ts — Run execution state
- defectStore.ts — Defect list state
- settingsStore.ts — LLM/ststem settings state
